# vim:ft=zsh

#bindkey '^i' complete-word
bindkey '^Y' all-matches
bindkey '^i' expand-or-complete-prefix
bindkey "\eOF" vi-end-of-line
bindkey "\eOH" vi-beginning-of-line
bindkey '^R' history-beginning-search-backward
bindkey '^N' history-beginning-search-forward
bindkey '\e[3~' delete-char
bindkey '\e[A' up-line-or-history
bindkey '\e[B' down-line-or-history
bindkey '^F' vi-forward-word
bindkey '^B' vi-backward-word
bindkey '^P' push-line-or-edit

##
## vi mode settings
## 
# bindkey -v # use vi-mode
# bindkey -M viins "§" which-command
# bindkey -M viins "^A" vi-beginning-of-line
# bindkey -M viins "^E" vi-end-of-line
# bindkey -M vicmd "U" undo
# bindkey -M viins "^U" undo
# bindkey -M vicmd "u" vi-undo-change
# bindkey -M viins '^R' push-line-or-edit
# bindkey -M viins '^W' vi-backward-kill-word
# bindkey -M viins '^O' insert-last-word
# zle -N vi-cmd-beginning-of-line
# zle -N vi-cw-first-on-line
# bindkey -M viins '^X' vi-cmd-beginning-of-line
# bindkey -M viins '^Xx' vi-cw-first-on-line
# bindkey -M viins '^xi'  incremental-complete-word
# bindkey -M viins '^xp'  predict-on
# bindkey -M viins '^x^p' predict-off

# vi-cmd-beginning-of-line () {
#     zle vi-cmd-mode
#     zle vi-beginning-of-line
# }

# vi-cw-first-on-line () {
#     vi-cmd-beginning-of-line
#     zle -U 'cw'
# }

